# ALIVE Studio Notes

## Today
- Bootstrapped ALIVE Studio MVP dashboard scaffold.

## Missing Details / Follow-ups
- Confirm per-repo preferred test command if not `npm run test`.
- Confirm per-repo preferred build command if not `npm run build`.
- Add richer command presets for runtime triage workflows.

## Commands to Remember
- `npm run dev`
- `open mind root`
- `run runtime tests`
